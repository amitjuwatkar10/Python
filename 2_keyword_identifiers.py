#Python Keywords and Identifiers
num = False
print(type(num)) #print type 
print(num)  # Prints value

#------------------------------------------------------

#Keyword lists 
#False	await	else	import	pass
#None	break	except	in	raise
#True	class	finally	is	return
#and	continue	for	lambda	try
#as	def	from	nonlocal	while
#assert	del	global	not	with
#async	elif	if	or	yield

#------------------------------------------------------

#Python Identifiers - Identifiers are the name given to variables, classes, methods, etc. For example,
language = 'Python'
print("Identifiers language value : " + language)

#------------------------------------------------------

#Rules for Naming an Identifier
'''Identifiers cannot be a keyword.
Identifiers are case-sensitive.
It can have a sequence of letters and digits. However, it must begin with a letter or _. The first letter of an identifier cannot be a digit.
It's a convention to start an identifier with a letter rather _.
Whitespaces are not allowed.
We cannot use special symbols like !, @, #, $, and so on.
'''
